# # Render.com blueprint — one-click deploy of LeadDesk Mini + Postgres.
# # Docs: https://render.com/docs/blueprint-spec
# services:
#   - type: web
#     name: leaddesk-mini
#     runtime: node
#     plan: free
#     buildCommand: npm install
#     startCommand: npm start
#     envVars:
#       - key: NODE_ENV
#         value: production
#       - key: ADMIN_USERNAME
#         value: admin
#       - key: ADMIN_PASSWORD
#         generateValue: true      # Render generates a strong password (view it in the dashboard)
#       - key: DATABASE_URL
#         fromDatabase:
#           name: leaddesk-db
#           property: connectionString

# databases:
#   - name: leaddesk-db
#     plan: free
